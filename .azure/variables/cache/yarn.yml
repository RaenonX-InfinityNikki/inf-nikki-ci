variables:
  # Can't be `YARN_CACHE_DIR` because yarn would assume it's for the yarn config in the environment variables
  - name: CACHE_DIR_YARN
    value: $(System.DefaultWorkingDirectory)/.yarn/cache
    readonly: true
